export const description =
  'On this page, we’ll dive into the different capture endpoints you can use to manage captures programmatically.'

# Capture

Capture

## The capture model

The capture model contains all the information about your captures.

### Properties

<Properties>
  <Property name="_id" type="string">
    Unique identifier for the capture.
  </Property>
  <Property name="activity" type="string">
    ...
  </Property>
  <Property name="description" type="string">
    ...
  </Property>
  <Property name="activity_date" type="datetime">
    ...
  </Property>
  <Property name="observer" type="string">
    ...
  </Property>
  <Property name="cluster" type="enum">
    `Servicing` `Thinking` `Reasoning` `Elementary` `Networking` `Generating Idea` `Technical` `Headman`
  </Property>
  <Property name="typology" type="Array<obj>">
    `serving` `caretaking` 

    ```
    {
      name: serving,
      xxx: ['easy', 'excelent']
      easy: true
      excelent: true
      enjoy: false
    }
    ```
  </Property>
  <Property name="institution" type="objectId">
    ...
  </Property>
  <Property name="isDraft" type="boolean">
    ...
  </Property>
  <Property name="documentLinks" type="Array<string>">
    ...
  </Property>
  <Property name="createdAt" type="timestamp">
    Timestamp of when the capture was created.
  </Property>
  <Property name="createdBy" type="objectId">
    Timestamp of when the capture was created.
  </Property>
  <Property name="updatedAt" type="timestamp">
    Timestamp of when the capture was updated.
  </Property>
</Properties>

---

## List all captures {{ tag: 'GET', label: '/v1/captures' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a paginated list of all your captures. By default, a maximum of ten captures are shown per page.

    ### Optional attributes

    <Properties>
      <Property name="page" type="integer">
        The page number of captures returned.
      </Property>
      <Property name="pageSize" type="integer">
        The page size number of captures returned.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/captures">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/captures', {
      params: {
        filter: {
          page:1,
          pageSize: 10,
          search: '',
          isDraft: true,
        },
        sort: 'date-'
      }
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "captures": [
        {
          "_id": "637b569149e0c02e1036c35a",
          "activity": "Catur",
          "clusters": [
            {
              "name": "A"
            },
            {
              "name": "B"
            }
          ],
          "isDraft": false,
          "date": "2022-01-01T00:00:00.000Z",
        },
      ],
      "pagination": {
        "page": 1,
        "pageSize": 1,
        "pageCount": 1,
        "totalDocument": 2
      }
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Create a capture {{ tag: 'POST', label: '/v1/captures' }}

<Row>
  <Col>

    This endpoint allows you to add a new capture.

    ### Required attributes

    <Properties>
      <Property name="name" type="string">
        The name for the capture.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/v1/captures">
    ```typescript {{ title: 'axios' }}
    const response = axios.post('/v1/captures', {
      name: 'Thinking',
      typologies: [
        {
          name: 'Analysing',
          suggestion: 'Lorem ipsum'
        },
        {
          name: 'Serving',
          suggestion: 'Lorem ipsum'
        }
      ]
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '201' }}
    {
      "_id": "637d83d15d2be122007524bf"
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Retrieve a capture {{ tag: 'GET', label: '/v1/captures/:id' }}

<Row>
  <Col>
    This endpoint allows you to retrieve a capture by providing their id.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/captures/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/captures/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "_id": "objectId",
      "files": ["https://...", "https://..."],
      "activity": "Catur",
      "description": "Lomba Catur 17 Agustus 2022",
      "date": "2022-08-17 15:00:00",
      "observer": "John Doe",
      "cluster": [
        {
          "name": "Thinking",
          "typologies": [
            {
              "name": "Analysing",
              "ikigai": ["easy", "excelent"]
            }
          ]
        },
        {
          "name": "Reasoning",
          "typologies": [
            {
              "name": "Evanluating",
              "ikigai": ["easy"]
            },
            {
              "name": "Exploring",
              "ikigai": ["easy", "excelent", "enjoy", "earn"]
            }
          ]
        }
      ],
      "institution": "Merlion School",
      "isDraft": false,
      "user_id": "objectId",
      "createdAt": "timestamp",
      "updatedAt": "timestamp",
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Update a capture {{ tag: 'PATCH', label: '/v1/captures/:id' }}

<Row>
  <Col>

    This endpoint allows you to perform an update on a capture.

    ### Optional attributes

    <Properties>
      <Property name="name" type="string">
        The name for the capture.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/captures/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/captures/637d83d15d2be122007524bf', {
      name: "Administrator"
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Delete a capture {{ tag: 'DELETE', label: '/v1/captures/:id' }}

<Row>
  <Col>
    This endpoint allows you to delete captures from your capture list.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="DELETE" label="/v1/captures/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.delete('/v1/captures/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Assign permissions {{ tag: 'PATCH', label: '/v1/captures/:id/assign-permissions' }}

<Row>
  <Col>

    This endpoint allows you to perform an update on a capture to assign permission.

    ### Optional attributes

    <Properties>
      <Property name="permissions" type="array<string>">
        The array of permission name for the capture.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/captures/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/captures/637d83d15d2be122007524bf', {
      permissions: [
        {
          name: 'create-user',
          assign: true
        },
        {
          name: 'read-user',
          assign: false
        }
      ]
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>
